## Öğretmenler için fazlamesai hesaplama

`nix-shell`

# sonrasında 

`python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt`

## veya 
` docker build . -t fazlamesai`

`docker run -itd  --name fazlamesai -p 5000:5000 fazlamesai:latest`
