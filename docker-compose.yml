version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - overtime_data:/data
    environment:
      - FLASK_DEBUG=0
      - SECRET_KEY=change-me-in-prod
      - DATABASE_FILE=/data/overtime.db

volumes:
  overtime_data:
